<div data-role="header">
  <h1>カードを選ぶ</h1>
</div>

<ul data-role="listview" data-theme="c">
  <% @cards.each do |card| %>
    <li>
    <a href=<%= battle_path %> id=<%= card.id %>>
      <%= image_tag(card.image) %>
      <h3><%= card.name %></h3>
    </a>
    </li>
  <% end %>
</ul>

